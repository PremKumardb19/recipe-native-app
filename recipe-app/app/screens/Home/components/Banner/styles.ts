import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 5,
    marginBottom:0
  },
  image: {
    width: "100%",
    height: 150,
    borderRadius: 10,
  },
});

export default styles;
